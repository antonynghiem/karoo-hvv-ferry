<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location permissions for GPS auto-detection -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Battery optimization: WAKE_LOCK removed - not needed with smart polling -->

    <application
        android:name=".HvvFerryApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HvvFerry"
        tools:targetApi="31">

        <!-- Main Configuration Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.HvvFerry">
            <intent-filter>
                <action android:name="dev.antonyng.hvvferry.MAIN" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Full Screen Ferry Times Activity -->
        <!-- taskAffinity="" puts it in its own task so finish() returns to the
             Karoo ride app instead of surfacing MainActivity. -->
        <activity
            android:name=".ui.FerryTimesActivity"
            android:exported="false"
            android:label="@string/ferry_times_title"
            android:taskAffinity=""
            android:theme="@style/Theme.HvvFerry" />

        <!-- Karoo Extension Service -->
        <service
            android:name=".extension.HvvFerryExtension"
            android:exported="true">
            <intent-filter>
                <action android:name="io.hammerhead.karooext.KAROO_EXTENSION" />
            </intent-filter>
            <meta-data
                android:name="io.hammerhead.karooext.EXTENSION_INFO"
                android:resource="@xml/extension_info" />
        </service>

        <meta-data
            android:name="io.hammerhead.karooext.MANIFEST_URL"
            android:value="https://gist.github.com/antonynghiem/0bbc39bddf14a5489adcbbf59b83b251/raw" />

    </application>

</manifest>
